<!--==============Logo & Menu Bar=================-->

<nav class="navbar navbar-default navbar-fixed-top" role="navigation"> 
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse"> <span class="sr-only">Toggle navigation</span> <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </button>
      <a class="navbar-brand" href="/"> <img src="static/img/logo-trans.png" alt="logo" width="70px"></a> </div>
    
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav navbar-right">
        
        <% if(user) { %>
          <% if(user.local.hasVerifiedEmail === false) { %>
            <li class="active"><a href="/">VERIFY EMAIL</a></li>
          <% } %>
        <% } %>
        
        <% if(user) { %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Stash: stashy.io<b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><a href="/profile">Change stash</a></li>
          </ul>
        </li>
      <% } %>
        
        <li class="active"><a href="/">HOME</a></li>
        <!--
        <li class="hidden-sm"><a href="features.html">FEATURES</a></li>
        <li><a href="contact.html">CONTACT</a></li>
        -->
        <li class="active"><a href="/usage">USAGE</a></li>
       
        
        <% if(user) { %>
          <% if(user.local.displayName !== 'notset') { %>
            <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= user.local.displayName %> <b class="caret"></b></a>
                  <ul class="dropdown-menu">
                    <% if(user.isAdmin == true) { %>
                      <li><a href="/admin">Admin</a></li>
                    <% } %>
                    <li><a href="/profile">Profile</a></li>
                    <li><a href="/logout">Logout</a></li>
                  </ul>
                </li>
          <% } else {%>
            
          <% } %>
        <% } else {%>
          <li><a href="#" role="button" data-toggle="modal" data-target="#Login">Login</a></li>
          <li><a href="#" role="button" data-toggle="modal" data-target="#Signup">Signup</a></li>
        <% } %>
      </ul>
      </li>
      </ul>
    </div>
    <!-- /.navbar-collapse --> 
    
  </div>
</nav>